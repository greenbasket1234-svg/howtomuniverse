# HOWTOM 유니버스 — 가짜 데이터 0으로 초기화 (변경 안내)

## 무엇이 바뀌었나

앱 곳곳에 하드코딩되어 있던 데모용 가짜 숫자(매출, ROAS, 클릭수, 광고비, 소재 성과 등)와
가짜 광고주/캠페인 목록을 모두 비우거나 0으로 바꿨습니다.
매체(Meta, 네이버, 구글 등) API가 실제로 연결되기 전까지는 화면이 빈 상태로 보이고,
연동이 완료되면 그 자리에 실제 데이터가 채워지는 구조입니다.

## 바뀐 파일 9개

| 파일 | 내용 |
|---|---|
| `server.mjs` | 데모 광고주 7곳, 데모 로그 3건 → 빈 배열 |
| `src/App.tsx` | 첫 실행 시 5·6·7월 샘플 데이터를 자동 생성하던 기능을 껐습니다. (설정 화면의 '샘플 데이터 생성' 버튼으로 필요할 때만 수동 생성 가능) |
| `src/data/advertisers.ts` | 광고주 8곳의 월 예산을 전부 0으로, 모든 매체 채널을 '미연동' 상태로 변경 |
| `src/data/brandReports.ts` | 6개 브랜드 × 7월 1~2일치 상세 리포트 숫자(노출/클릭/광고비/DB/매출)를 전부 0으로, "실제 데이터" 표시(`hasRealData`)를 전부 꺼서 화면에 "(예시)" 라벨이 뜨도록 변경 |
| `src/data/creativeLibrary.ts` | 소재 라이브러리 샘플 11건 → 빈 배열 |
| `src/data/creativePerformance.ts` | 소재 성과 샘플 10건 → 빈 배열 |
| `src/data/keywordAnalysisMock.ts` | 키워드 분석 시 매번 무작위로 생성되던 가짜 노출/클릭/전환 숫자 → 항상 빈 배열 반환 |
| `src/data/operationsMock.ts` | 캠페인 7건, 퍼널 성과 5건, 일정 5건 → 전부 빈 배열 |
| `src/pages/AlertsLogsPage.tsx` | 실제 로그 API 실패 시 보여주던 가짜 로그 3건 → 빈 배열 |

## 적용 방법

1. 로컬(또는 GitHub 웹)에서 기존 프로젝트 폴더를 여세요.
2. 이 zip 안의 9개 파일을 **같은 경로 그대로** 덮어쓰세요.
   (예: `src/data/advertisers.ts` → 기존 `src/data/advertisers.ts` 자리에 그대로 교체)
3. 터미널에서 순서대로 실행하세요.

```
git add .
git commit -m "fix: 데모 샘플 데이터를 0으로 초기화, 매체 연동 전까지 빈 상태로 표시"
git push
```

4. Railway가 자동으로 재배포합니다 (30초~1분 소요).

## 참고

- `VITE_REPOSITORY_MODE` 환경변수가 이미 앱에 준비되어 있습니다. 기본값은 `mock`이고,
  실제 백엔드가 준비되면 `api`로 바꾸고 `VITE_API_BASE_URL`을 설정하면 실제 데이터 소스로 전환됩니다.
- 위 변경은 어디까지나 "화면에 뜨는 가짜 숫자를 0으로" 만든 것이며, 실제 매체 API 연동
  자체(Meta/네이버/구글 등 서버 Connector 구현)는 README 6번 섹션에 있는 순서대로 별도로 진행해야 합니다.
